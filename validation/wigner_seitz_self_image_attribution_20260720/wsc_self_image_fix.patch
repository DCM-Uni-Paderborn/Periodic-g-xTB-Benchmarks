diff --git a/src/tblite/wignerseitz.f90 b/src/tblite/wignerseitz.f90
index 6fe48a2..fef643a 100644
--- a/src/tblite/wignerseitz.f90
+++ b/src/tblite/wignerseitz.f90
@@ -88,6 +88,9 @@ subroutine get_pairs(iws, trans, rij, list)
    logical :: mask(size(list))
    real(wp) :: dist(size(list)), vec(3), r2
    integer :: itr, img, pos
+   integer, allocatable :: orig(:)
+
+   allocate(orig(size(list)))
 
    iws = 0
    img = 0
@@ -100,6 +103,7 @@ subroutine get_pairs(iws, trans, rij, list)
       if (r2 < thr) cycle
       img = img + 1
       dist(img) = r2
+      orig(img) = itr
    end do
 
    if (img == 0) return
@@ -110,7 +114,7 @@ subroutine get_pairs(iws, trans, rij, list)
    mask(pos) = .false.
 
    iws = 1
-   list(iws) = pos
+   list(iws) = orig(pos)
    if (img <= iws) return
 
    do
@@ -118,7 +122,7 @@ subroutine get_pairs(iws, trans, rij, list)
       if (abs(dist(pos) - r2) > tol) exit
       mask(pos) = .false.
       iws = iws + 1
-      list(iws) = pos
+      list(iws) = orig(pos)
    end do
 
 end subroutine get_pairs
