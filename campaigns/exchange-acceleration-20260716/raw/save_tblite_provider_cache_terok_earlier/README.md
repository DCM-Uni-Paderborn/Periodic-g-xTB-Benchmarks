# Preserved earlier terok output

This directory preserves an earlier 30/30 Exchange-suite CTest record.  It is
not used as byte-exact qualification provenance because the executable at its
recorded path was replaced while the long-running test was in progress.  The
fresh local focused run in `../save_tblite_provider_forward/` is tied to an
exact source snapshot, patch, binary hash, and build configuration and is the
authoritative qualification record.
